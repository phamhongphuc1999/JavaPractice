services:
    mysql_container:
        image: mysql:latest
        container_name: "ed_sql_container"
        environment:
            MYSQL_ROOT_PASSWORD: java
        ports:
            - "3307:3306"
        volumes:
            - "./event-driven-database/setup.sql:/docker-entrypoint-initdb.d/1.sql"
